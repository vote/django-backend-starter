dist: bionic
language: python
python:
  - "3.8"

before_install:
  - touch .env

install:
  - docker-compose build

before_script:
  - docker-compose up -d

script:
  # Put test code here
  - make mypy

notifications:
  email: false
